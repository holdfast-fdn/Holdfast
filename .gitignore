# Python
__pycache__/
*.pyc
.venv/

# Node (for the future bot/indexer)
node_modules/
dist/

# Foundry (for the future contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/

# OS / editor
.DS_Store
Thumbs.db

# Secrets — never commit keys (see CLAUDE.md)
.env
.env.*
*.key

# GM service runtime artifacts — keys & tick state never enter the repo
gm/keys*.json
gm/state/

# Purchased art packs — LOCAL ONLY, never push to the public repo
# (Moon Tribe 2D Floating Islands; low-poly 3D pack CC BY-ND)
ui/assets/*.png
ui/assets/*.jpg
ui/assets/models/
*.unitypackage
PNG.zip
PSD.zip
Models Islands.zip
Screenshot*.png
