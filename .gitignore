# Python
__pycache__/
*.pyc
.venv/

# Node (for the future bot/indexer)
node_modules/
dist/

# Foundry (for the future contracts/)
contracts/out/
contracts/cache/
contracts/broadcast/

# OS / editor
.DS_Store
Thumbs.db

# Secrets — never commit keys (see CLAUDE.md)
.env
.env.*
*.key

# GM service runtime artifacts — keys & tick state never enter the repo
gm/keys*.json
gm/state/

# Purchased art packs.
# The 2D floating-isles sprites the WEB UI serves are committed so the site
# deploys correctly (holdfast.foundation/assets/*) — owner-approved 2026-06-14.
# The 3D low-poly pack (CC BY-ND) and unused art stay LOCAL ONLY:
ui/assets/models/
ui/assets/tower-*.png
ui/assets/*.jpg
*.unitypackage
PNG.zip
PSD.zip
Models Islands.zip
Screenshot*.png
source/
